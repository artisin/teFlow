notifications:
  email: false
language: node_js
env: TRAVIS_CI=true
sudo: false
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH
cache:
  yarn: true
before_script:
  - yarn run link
script: yarn test